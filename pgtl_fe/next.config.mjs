/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: false, //Disables the dev tools UI completely
    // devIndicators: {
    //     buildActivity: false, // Disables build activity indicator
    //   },

  };

export default nextConfig;
